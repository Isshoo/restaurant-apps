import Favorite from '../view/pages/favorite';
import Home from '../view/pages/home';

const routes = {
  '/': Home, // default page
  '/home': Home,
  '/favorite': Favorite,
};

export default routes;
